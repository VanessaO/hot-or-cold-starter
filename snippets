/*--- Runs whole game ---*/
  	/*function guessingGame() {
  		function hotCold(num) {
  			if(num <= num + 50) {
  				$( "#feedback" ).text( "Ice Cold" );
  			}
  			else if(num <= num + 30) {
  				$( "#feedback" ).text( "Cold" );
  			}
  			else if(num <= num + 20) {
  				$( "#feedback" ).text( "Warm" );
  			}
  			else if(num <= num + 10) {
  				$( "#feedback" ).text( "Hot" );
  			}
  			else if(num <= num + 1) {
  				$( "#feedback" ).text( "Very Hot" );
  			}
  		} */
  		
  		var secretNum = function() {
  			var  randomNum = Math.floor(Math.random() * 101);
  			return randomNum;
  		}

  			
  		

  		function getNum() {
  			var guessedNum = document.getElementById("userGuess").value;

  			if (guessedNum != "") {
  				//document.getElementById('feedback').innerHTML = "Correct!";
  				alert("You entered: " + guessedNum.value);
  				//$( "#feedback" ).text( "Correct!" );
  			} else {
  				//testMultiply(guessedNum);
  				alert("Put in value please");
  				//return hotCold(x);
  			} //else if (isNaN(guessedNum)) {
  				//alert(guessedNum + " is not a number value");
  			//}
  		}

  		/*function testMultiply(x) {
  			var multipliedNum = x * 2;
  			alert(multipliedNum); */
  		}
  		//}

  		

  		//secretNum(guessedNum);
  	//}



1. This creates a alert from the number typed into the input box #userGuess
	
	$("#guessButton").click(function getNum() {
  		var guessedNum = document.getElementById("userGuess");

  			if (guessedNum.value != "0") {
  				alert("You entered: " + guessedNum.value);	
  			} else {
  				alert("Put in value please");
  			} 
  	});

2. Compares a generated # to input value, EVERY time suubmit button clicked. Meaning, it generates a new randomNum with every click.
  	$("#guessButton").click(function compareNum() {
  		var guessedNum = parseInt(document.getElementById("userGuess").value);
  		var  randomNum = Math.floor(Math.random() * 101);
  			if (guessedNum == randomNum) {
  				document.getElementById("feedback").innerHTML = "Correct!";
  			} 
  			else if(guessedNum <= randomNum + 1) {
  				document.getElementById("feedback").innerHTML = "Very Hot!";
  			}
  			else if(guessedNum <= randomNum + 10) {
  				document.getElementById("feedback").innerHTML = "Hot!";
  			}
  			else if(guessedNum <= randomNum + 20) {
  				document.getElementById("feedback").innerHTML = "Warm!";
  			}
  			else if(guessedNum <= randomNum + 30) {
  				document.getElementById("feedback").innerHTML = "Cold!";
  			}
  			else if(guessedNum <= randomNum + 50) {
  				document.getElementById("feedback").innerHTML = "Ice Cold!";
  			}
  			else {
  				//testMultiply(guessedNum);
  				//alert("Put in bigger value please");
  				alert("Please enter a numeric value.");
  				//return hotCold(x);
  			} //else if (isNaN(guessedNum)) {
  				//alert(guessedNum + " is not a number value");
  		//function keepScore() {}
  	});
















